# Maison Noir — Fine Jewelry SPA

A single-page jewelry shop built with React, Tailwind CSS, and Framer Motion.
No backend, no database — all product data lives in `src/data/products.js`.

## Setup

```bash
npm install
npm run dev       # start dev server at http://localhost:5173
npm run build     # production build to /dist
```

## Structure

See the folder tree in the accompanying chat message for a full breakdown of
every file and its purpose.
